import 'package:flutter/material.dart';

class MysListSelect extends StatelessWidget {
  const MysListSelect({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
